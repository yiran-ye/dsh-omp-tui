# Roadmap

以下能力明确延后，不阻塞当前 MVP：

- `/model` 完整 Provider 管理
- `/presets` 图形化切换
- `/context` 完整上下文分析
- `/plugins` 插件树检查
- 本地 Shell Mode
- `@` 文件补全
- 图片显示
- 多 Session Tab

后续还可增加 Reasoning 逐块折叠状态持久化、工具 diff 语法高亮、上下文压力
Projection 的精细状态栏，以及更多终端键盘协议兼容性快照。
